# Core Django
Django
asgiref
sqlparse
tzdata

# Django REST Framework
djangorestframework
djangorestframework-simplejwt
channels
channels-redis>=4.2

# Auth system
django-allauth
dj-rest-auth

# Social auth dependencies
requests
oauthlib
requests-oauthlib

# Translation
googletrans==4.0.2
langdetect

# Security / encryption
cryptography>=42.0.0

# Static & media
Pillow
whitenoise==6.7.0
filetype>=1.2.0
django-storages[boto3]>=1.14
boto3>=1.34

# CORS
django-cors-headers

# Environment variables
python-dotenv

# E-mail (API Resend, remplace SMTP lorsque RESEND_API_KEY est défini)
resend>=2.0.0
currencyconverter
Babel
pywebpush

# Database
dj-database-url
psycopg2-binary

# Production server
gunicorn

# Moderation & limits
better-profanity
django-ratelimit

# Médias / modération (échantillon vidéo + optional ONNX NSFW)
opencv-python-headless>=4.9,<5
numpy>=1.26
onnxruntime>=1.17

# Seed data (noms / profils synthétiques)
Faker>=24.0.0

# Email
resend

# Ads / async / cache Redis (optionnel si REDIS_URL défini)
celery>=5.3
tenacity>=8.2
posthog>=3.0
sentry-sdk[django]>=2.0
redis>=5.0
django-redis>5.4
daphne

# Environment variables
dotenv-cli
python-dotenv

# OpenTelemetry (optionnel — pip install si OTEL_EXPORTER_OTLP_ENDPOINT défini)
# opentelemetry-api
# opentelemetry-sdk
# opentelemetry-exporter-otlp-proto-http
# opentelemetry-instrumentation-django

# Recherche Phase 2 (Typesense)
typesense>=0.21.0